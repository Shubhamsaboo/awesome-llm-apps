# No external dependencies required
# DNA Memory uses only Python standard library

# Optional: For enhanced vector search (not required for basic functionality)
# numpy>=1.20.0
# scikit-learn>=0.24.0
