# No third-party dependencies — this tutorial uses the Python standard library only
# (urllib, json, re, dataclasses). Nothing to install.
#
# Backend is local-first via Ollama (no API key needed):
#     ollama serve && ollama pull qwen2.5:14b
#
# The OpenAI fallback (LOOPR_PROVIDER=openai) also uses urllib directly — no SDK required.
