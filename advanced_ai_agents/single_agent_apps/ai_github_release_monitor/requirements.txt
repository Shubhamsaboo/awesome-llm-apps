# Runtime dependencies — install with: pip install -r requirements.txt
anthropic>=0.40.0        # Claude LLM for semantic release analysis
httpx>=0.27.0            # GitHub REST API calls (lightweight, async-capable)
rich>=13.0.0             # CLI formatting (optional — degrades gracefully)
streamlit>=1.30.0        # Web UI
